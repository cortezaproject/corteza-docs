    <link rel="icon" href="{{siteRootPath}}/favicon32x32.png" type="image/x-icon">

    <link rel="stylesheet" href="{{uiRootPath}}/css/site-extra.css">
    {{#if env.ALGOLIA_API_KEY}}
    <!-- downloaded from CDN -->
    <link rel="stylesheet" href="{{uiRootPath}}/css/vendor/docsearch.min.css">
    <link rel="stylesheet" href="{{uiRootPath}}/css/vendor/docsearch.css">
    {{/if}}

    <!-- DocSearch meta -->
    <meta name="docsearch:language" content="en" />
    <meta name="docsearch:version" content="{{page.version}}" />
    <meta name="docsearch:module" content="{{page.module}}" />

    <meta name="docsearch:index:key-section" content="{{page.attributes.key-section}}" />

    {{#if page.attributes.noindex}}
    <meta name="robots" content="noindex">
    {{/if}}